var friends = ["Lars", "Jan", "Peter", "Bo", "Frederik"];

for( var i = 0; i < friends.length; i++ ) {  
    alert( i ); // This loop will trigger three alerts, reading "0", "1", and "2" respectively.
}